# 🚀 Vityaz 3D - Setup Guide\n\n## Быстрая Установка\n\n### 👋 Windows (PowerShell)\n\n```powershell\n# запустите как администратор\n.\\setup.ps1\n```\n\n### 👋 Linux / macOS (Bash)\n\n```bash\n# сначала дать права на исполнение\nchmod +x setup.sh\n.\n/setup.sh\n```\n\n---\n\n## Мануальная Установка\n\nОсновные требования:\n\n### 1. Node.js\n\n- Установите Node.js v18+ с [nodejs.org](https://nodejs.org)\n- Проверьте: `node --version` и `npm --version`\n\n### 2. Клонирование репозитория\n\n```bash\ngit clone https://github.com/kaylas000/vityaz-3d.git\ncd vityaz-3d\n```\n\n### 3. Установка зависимостей\n\n#### Backend\n\n```bash\ncd backend\nnpm install --legacy-peer-deps\n```\n\n#### Frontend\n\n```bash\ncd ../frontend\nnpm install --legacy-peer-deps\n```\n\n---\n\n## 🏃 Запуск Проекта\n\n### Backend (Node.js/Express)\n\n```bash\ncd backend\nnpm run dev\n\n# или\nnpm start\n```\n\n🔗 **URL**: `http://localhost:5000`\n\n### Frontend (React/Vue/Angular)\n\n```bash\ncd frontend\nnpm run dev\n\n# или\nnpm start\n```\n\n🔗 **URL**: `http://localhost:3000`\n\n### Одновременно в разных терминалах:\n\n**Terminal 1** - Backend:\n```bash\ncd backend && npm run dev\n```\n\n**Terminal 2** - Frontend:\n```bash\ncd frontend && npm run dev\n```\n\n---\n\n## ⚙️ Команды\n\n### Backend\n\n```bash\nnpm run dev        # расработка\nnpm run build      # состав\nnpm test           # тесты\nnpm run lint       # проверка кода\n```\n\n### Frontend\n\n```bash\nnpm run dev        # расработка\nnpm run build      # продакшн сборка\nnpm test           # тесты\nnpm run lint       # проверка кода\n```\n\n---\n\n## 📄 Гит\u0445аб Actions CI/CD\n\nПайплайн автоматически распускается при:\n\n- ЦPush в `main` или `develop`\n- При создании PR\n\n### Настройка:\n\n`.github/workflows/ci-cd.yml` содержит:\n\n- 📦 `npm install` - установка зависимостей\n- 🔍 `npm run lint` - проверка кода\n- 🧪 `npm test` - тесты\n- 🏗️ `npm run build` - состав\n- 🔐 Security scan - проверка уязвимостей\n\n---\n\n## ⚠️ Ошибки\n\n### `npm ERR! peer dep missing`\n\nРешение:\n```bash\nnpm install --legacy-peer-deps\n```\n\n### `node_modules` на диске\n\n`node_modules/` донамерно не коммитится в гит. Для чистки:\n\n```bash\nrm -rf backend/node_modules frontend/node_modules\nnpm install --legacy-peer-deps  # нав\u0441та\n```\n\n---\n\n## 📇 Важно\n\n- `.env` файлы не коммитятся (check `.gitignore`)\n- `.env.example` содержит работы е переменные\n- Копируйте: `cp .env.example .env` и заполните\n\n---\n\n## 🙋 Помощь\n\nЕсли что-то не работает:\n\n1. Очистите npm кэш: `npm cache clean --force`\n2. Удалите `node_modules` и `package-lock.json`\n3. Переустановите: `npm install --legacy-peer-deps`\n4. Напишите issue в [GitHub Issues](https://github.com/kaylas000/vityaz-3d/issues)\n\n---\n\n**🌟 Happy Coding!**\n"
<parameter name="sha">