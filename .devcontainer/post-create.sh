#!/bin/bash\n\necho \"\"\necho \"╔════════════════════════════════════════════════════════════════════════════════╗\"\necho \"║  🚀 GitHub Codespaces Setup - Vityaz 3D                                           ║\"\necho \"╚════════════════════════════════════════════════════════════════════════════════╝\"\necho \"\"\n\necho \"[1/3] 📦 Installing Backend dependencies...\"\nif [ -f \"backend/package.json\" ]; then\n    cd backend\n    npm install --legacy-peer-deps\n    if [ $? -eq 0 ]; then\n        echo \"✅ Backend dependencies installed\"\n    else\n        echo \"⚠️  Backend installation had issues (continuing anyway)\"\n    fi\n    cd ..\nelse\n    echo \"⚠️  backend/package.json not found, skipping\"\nfi\n\necho \"\"\n\necho \"[2/3] 📦 Installing Frontend dependencies...\"\nif [ -f \"frontend/package.json\" ]; then\n    cd frontend\n    npm install --legacy-peer-deps\n    if [ $? -eq 0 ]; then\n        echo \"✅ Frontend dependencies installed\"\n    else\n        echo \"⚠️  Frontend installation had issues (continuing anyway)\"\n    fi\n    cd ..\nelse\n    echo \"⚠️  frontend/package.json not found, skipping\"\nfi\n\necho \"\"\n\necho \"[3/3] 🎯 Verification...\"\necho \"\"\n\necho \"╔════════════════════════════════════════════════════════════════════════════════╗\"\necho \"║  ✅ SETUP COMPLETED!                                                            ║\"\necho \"╚════════════════════════════════════════════════════════════════════════════════╝\"\necho \"\"\n\nif [ -d \"backend/node_modules\" ]; then\n    echo \"✅ Backend ready - run: cd backend && npm run dev\"\nelse\n    echo \"⚠️  Backend node_modules not found\"\nfi\n\nif [ -d \"frontend/node_modules\" ]; then\n    echo \"✅ Frontend ready - run: cd frontend && npm run dev\"\nelse\n    echo \"⚠️  Frontend node_modules not found\"\nfi\n\necho \"\"\necho \"📝 Next steps:\"\necho \"  Terminal 1: cd backend && npm run dev    (runs on port 5000)\"\necho \"  Terminal 2: cd frontend && npm run dev   (runs on port 3000)\"\necho \"\"\necho \"🔗 Ports will be automatically forwarded and accessible in your browser!\"\necho \"\"\n"
<parameter name="sha">